<%= form_with(model: name, local: true) do |form| %>
  <% if name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(name.errors.count, "error") %> prohibited this name from being saved:</h2>

      <ul>
      <% name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :genre %>
    <%= form.text_field :genre, id: :name_genre %>
  </div>

  <div class="field">
    <%= form.label :year_released %>
    <%= form.number_field :year_released, id: :name_year_released %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :name_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
